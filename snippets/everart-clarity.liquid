{%- unless request.design_mode -%}
<script type="text/javascript">
  (function () {
    function loadClarity() {
      if (window.__eaClarityLoaded) return;
      window.__eaClarityLoaded = true;
      (function(c,l,a,r,i,t,y){
        c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
        t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
        y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
      })(window, document, "clarity", "script", "wuyszxqdfs");
    }
    function check() {
      var cp = window.Shopify && window.Shopify.customerPrivacy;
      if (cp && cp.analyticsProcessingAllowed && cp.analyticsProcessingAllowed()) {
        loadClarity();
        return true;
      }
      return false;
    }
    if (!check()) {
      document.addEventListener('visitorConsentCollected', function () { check(); });
      window.Shopify = window.Shopify || {};
      if (window.Shopify.loadFeatures) {
        window.Shopify.loadFeatures(
          [{ name: 'consent-tracking-api', version: '0.1' }],
          function (e) { if (!e) check(); }
        );
      }
    }
  })();
</script>
{%- endunless -%}